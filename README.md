# cpp-matrix-algorithms
A comprehensive collection of 50+ C++ solutions focused on matrix operations and advanced algorithmic problem-solving. Part of my journey to master logic building and software engineering fundamentals.
# C++ Mastery: Advanced Matrix & Array Algorithms 🚀

This repository is a comprehensive collection of C++ programs focusing on **2D Arrays (Matrices)**, **Algorithm Logic**, and **Computational Problem-Solving**. It covers operations ranging from basic traversal to complex mathematical matrix manipulations.

📂 **Project Structure**
The project is organized into modular `.cpp` files, each designed with clean logic and reusable functions.

---

## 📋 List of Challenges & Solutions
*I add 10 new challenges daily. Here is the current progress:*

| # | Professional File Name | Description | Key Concept |
| :---: | :--- | :--- | :--- |
| 01 | `Matrix-Random-Filler.cpp` | Fill a 3x3 matrix with random numbers (1-100). | `Randomization` |
| 02 | `Matrix-Row-Sum.cpp` | Calculate and print the sum of each row individually. | `Row Traversal` |
| 03 | `Matrix-Row-Sum-Array.cpp` | Store each row's sum into a separate 1D array. | `Data Storage` |
| 04 | `Matrix-Col-Sum.cpp` | Calculate and print the sum of each column. | `Column Traversal` |
| 05 | `Matrix-Col-Sum-Array.cpp` | Store each column's sum into a separate 1D array. | `Abstraction` |
| 06 | `Matrix-Ordered-Filler.cpp` | Fill a matrix with ordered numbers (1 to 9). | `Sequential Loops` |
| 07 | `Matrix-Transpose.cpp` | Transpose a matrix (swap rows with columns). | `Transposition` |
| 08 | `Matrix-Multiplication.cpp` | Multiply two 3x3 matrices and display the result. | `Math Algorithms` |
| 09 | `Matrix-Middle-Display.cpp` | Extract and print the middle row and middle column. | `Indexing Logic` |
| 10 | `Matrix-Total-Sum.cpp` | Calculate the sum of all elements in the matrix. | `Accumulation` |

> **Tip:** To add more, just copy the last row and update the details!

---

## 🔍 Featured Solutions (Detailed Breakdown)

### 1. Matrix Transpose (`Matrix-Transpose.cpp`)
* **Goal:** Convert a matrix's rows into columns and vice versa.
* **Logic:** Uses a nested loop where `Arr2[j][i] = Arr1[i][j]`.
* **Key Functions:** `FillMatrix()`, `TransposeMatrix()`, `PrintMatrix()`.

### 2. Matrix Row Sum to Array (`Matrix-Row-Sum-Array.cpp`)
* **Goal:** Isolate the summation logic and store results for later use.
* **Technical Skill:** Demonstrates how to pass a 1D array to a function to hold results from a 2D array operation.

---

## ⚙️ How to Use
1. Clone the repo: `git clone https://github.com/YourUsername/cpp-matrix-algorithms.git`
2. Open any `.cpp` file in **Visual Studio** or **VS Code**.
3. Compile using `g++` or your preferred compiler.

---
*Created with ❤️ by [Your Name]*
